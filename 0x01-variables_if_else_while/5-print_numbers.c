#include <stdio.h>
/**
 * main - Entry point
 * Description: A C program that prints with printf function.
 * Return: Always 0 (success000)
*/
int main(void)
{
	int n = 48;

	while (n <= 57)
	{
		printf("%i", n);
		n++;
	}
	putchar('\n');
	return (0);
}
